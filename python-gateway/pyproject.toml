[project]
name = "a2rchitech-python-gateway"
version = "0.1.0"
description = "Python gateway for running external tools"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.24",
    "pydantic>=2.0",
]

[project.optional-dependencies]
cli = ["click>=8.1"]
